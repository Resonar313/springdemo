CREATE TABLE MEMBER (
    -> ID BIGINT NOT NULL AUTO_INCREMENT,
    -> NAME VARCHAR(255),
    -> PRIMARY KEY(ID)
    -> );

insert into member (
-> name) values('spring');

insert into member (name) values('spring2');